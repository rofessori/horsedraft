// Intentionally empty for now: the renderer needs nothing from Node. (CommonJS on purpose, see main.cts.)
// When native features are added (save dialogs, sounds from disk), expose them here
// with contextBridge.exposeInMainWorld and keep nodeIntegration off.
export {};
